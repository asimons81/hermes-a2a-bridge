# Hermes-Owned File Reference Fixtures

These fixtures describe a file already staged by the local Hermes file command. Public metadata, task artifacts, and replay events include only opaque IDs, safe names, checksums, sizes, and a localhost byte route.

The fixture payloads do not include internal storage fields or local machine paths.
