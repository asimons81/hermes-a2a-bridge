# SDK 1.0.3 File-Part Rejection Fixture

These fixtures mirror the SDK 1.1.0 file-part rejection shape. SDK 1.0.3 can construct `raw`, `filename`, and `url` parts, but Hermes rejects them before task creation.

The Agent Card fixture keeps only text and JSON modes. It does not advertise file, image, audio, or video input support.
